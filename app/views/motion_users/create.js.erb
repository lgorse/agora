$(function(){
	var motion = $(".motion#<%= @motion.id %>");
	motion.find(".join").html("<%= escape_javascript(render 'motion_users/button', :motion => @motion) %>");
	motion.find(".voter_count").html('<%= "#{@motion.users.count} voters" %>');
});