$(function(){
	var motion = $(".motion#<%= @motion.id %>");
	motion.append("<%= escape_javascript(render 'replies/new_reply_form') %>");
	motion.find(".reply_link").click(function(e){
		if (motion.find(".new_reply").length){
			e.preventDefault();
			e.stopPropagation();
		}
	});
});